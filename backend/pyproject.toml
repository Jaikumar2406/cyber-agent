[project]
name = "aegis"
version = "0.1.0"
description = "AEGIS - Adaptive Application Security & Cryptographic Risk Platform (Phase 0)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
filterwarnings = [
    "ignore::DeprecationWarning",
]